# Python CLI tools migrated from uv tool environments.
# Existing user-site libraries are outside this CLI migration and remain untouched.

[tools]
"pipx:yt-dlp" = "2026.8.19"
"pipx:zotero-mcp-server" = "0.11.0"
"pipx:pymupdf" = "1.28.2"
"pipx:weasyprint" = "69.0"
