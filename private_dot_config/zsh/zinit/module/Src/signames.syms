E#ifndef have_Src_signames_globals
E#define have_Src_signames_globals
E
E#define sigmsg(sig) ((sig) <= SIGCOUNT ? sig_msg[sig] : "unknown signal")
L#define sigmsg(sig) ((sig) <= SIGCOUNT ? sig_msg[sig] : "unknown signal")
Xsig_msg
Eextern mod_import_variable char*sig_msg[SIGCOUNT+2];
Eextern char*sigs[SIGCOUNT+4];
E
E#endif /* !have_Src_signames_globals */
