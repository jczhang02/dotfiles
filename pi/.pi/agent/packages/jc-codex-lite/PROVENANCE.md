# Provenance

Initial Rust path-tool sources and helper binaries were extracted from the installed upstream Codex conversion package version `2.0.1`, then reduced to the local minimal surface maintained here.

The active Pi configuration loads this local package path directly and does not install or load the upstream package.
